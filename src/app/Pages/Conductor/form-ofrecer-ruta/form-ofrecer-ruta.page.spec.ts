import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormOfrecerRutaPage } from './form-ofrecer-ruta.page';

describe('FormOfrecerRutaPage', () => {
  let component: FormOfrecerRutaPage;
  let fixture: ComponentFixture<FormOfrecerRutaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormOfrecerRutaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
