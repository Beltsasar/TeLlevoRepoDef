import { ComponentFixture, TestBed } from '@angular/core/testing';
import { MostraDetallesRutaCPage } from './mostra-detalles-ruta-c.page';

describe('MostraDetallesRutaCPage', () => {
  let component: MostraDetallesRutaCPage;
  let fixture: ComponentFixture<MostraDetallesRutaCPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(MostraDetallesRutaCPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
